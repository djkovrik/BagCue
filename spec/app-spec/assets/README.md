# AppSpec asset references

No external source artwork is supplied. ASSET-001 authorizes original vector creation from the approved BagCue brief in `app-spec.json` and `design.md`. ASSET-002 through ASSET-017 are approved Material Symbols Rounded adaptations whose exact provenance and delivered hashes must be recorded outside this frozen tree in `docs/assets/asset-manifest.json`.

Do not place generated runtime outputs or mutable delivery evidence in this directory.

