platform :ios, '16.2'

source 'https://cdn.cocoapods.org/'

target 'iosApp' do
  use_frameworks! :linkage => :static

  # Analytics only: do not add AdSupport/IDFA-capable Firebase products.
  pod 'FirebaseAnalytics/Core', '12.18.0'
  # Linked for build parity. BagCue iOS supplies no ad unit and never requests an ad.
  pod 'YandexMobileAds', '8.4.0'
end
